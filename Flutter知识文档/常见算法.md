# 算法学习

## 算法初见

常用算法可以按 **类型** 来分，我给你列一份比较全的清单（不分语言，都是通用的算法思想）：

------

### 一、排序算法

- 冒泡排序（Bubble Sort）
- 选择排序（Selection Sort）
- 插入排序（Insertion Sort）
- 希尔排序（Shell Sort）
- 快速排序（Quick Sort）
- 归并排序（Merge Sort）
- 堆排序（Heap Sort）
- 计数排序（Counting Sort）
- 桶排序（Bucket Sort）
- 基数排序（Radix Sort）

------

### 二、查找与搜索算法

- 线性查找（Linear Search）
- 二分查找（Binary Search）
- 插值查找（Interpolation Search）
- 三分查找（Ternary Search）
- 深度优先搜索（DFS）
- 广度优先搜索（BFS）
- 双向 BFS（Bidirectional BFS）
- A* 搜索（A* Search）
- Dijkstra 最短路径算法
- Bellman-Ford 算法
- Floyd-Warshall 全源最短路径
- Johnson 算法

------

### 三、图论相关

- 并查集（Union-Find / Disjoint Set Union, DSU）
- Kruskal 最小生成树算法
- Prim 最小生成树算法
- 拓扑排序（Topological Sort）
- 二分图匹配（匈牙利算法、Hopcroft–Karp 算法）
- Tarjan 强连通分量（SCC）
- Kosaraju 强连通分量
- 最小割 / 最大流（Ford–Fulkerson、Edmonds–Karp、Dinic）

------

### 四、动态规划（DP）

- 背包问题（0-1 背包、完全背包、多重背包）
- 最长公共子序列（LCS）
- 最长上升子序列（LIS）
- 编辑距离（Edit Distance / Levenshtein Distance）
- 矩阵链乘（Matrix Chain Multiplication）
- 股票买卖问题（Best Time to Buy and Sell Stock）
- 最大子数组和（Kadane’s Algorithm）
- 区间 DP
- 状态压缩 DP

------

### 五、分治与贪心

- 分治法（Divide and Conquer）
- 贪心算法（Greedy Algorithm）
- 霍夫曼编码（Huffman Coding）
- 活动选择（Activity Selection）

------

### 六、数学与数论

- 欧几里得算法（GCD）
- 扩展欧几里得算法（Extended GCD）
- 埃拉托斯特尼筛法（Sieve of Eratosthenes）
- 快速幂（Fast Power / Binary Exponentiation）
- 快速乘（Fast Multiplication）
- 模逆元（Modular Inverse）
- 组合数计算（Combinations）
- 中国剩余定理（CRT）
- Miller–Rabin 素性测试
- Pollard Rho 因数分解

------

### 七、数据结构相关算法

- 栈 / 队列 基本操作
- 优先队列（Priority Queue / Heap）
- 线段树（Segment Tree）
- 树状数组（Binary Indexed Tree, Fenwick Tree）
- 跳表（Skip List）
- Trie 树（前缀树）
- KMP 字符串匹配
- Rabin–Karp 字符串匹配
- Z-Algorithm
- Manacher 回文串算法
- AC 自动机（Aho–Corasick Automaton）

## 排序算法

### 1. 冒泡排序（Bubble Sort）

```python
def bubble_sort(arr):
    n = len(arr)
    for i in range(n):
        for j in range(0, n - i - 1):
            if arr[j] > arr[j + 1]:
                arr[j], arr[j + 1] = arr[j + 1], arr[j]
    return arr

print(bubble_sort([5, 3, 8, 4, 2]))
# [2, 3, 4, 5, 8]
```

### 2. 插入排序（Insertion Sort）

```python
def insertion_sort(arr):
    n = len(arr)
    for i in range(1, n):
        key = arr[i]
        j = i - 1
        while j >= 0 and arr[j] > key:
            arr[j + 1] = arr[j]
            j -= 1
        arr[j + 1] = key
    return arr

print(insertion_sort([5, 3, 8, 4, 2]))
# [2, 3, 4, 5, 8]

```

### 3. 快速排序（Quick Sort）

```python
def quick_sort(arr):
    if len(arr) <= 1:
        return arr
    pivot = arr[len(arr) // 2]
    left = [x for x in arr if x < pivot]
    mid = [x for x in arr if x == pivot]
    right = [x for x in arr if x > pivot]
    return quick_sort(left) + mid + quick_sort(right)

print(quick_sort([5, 3, 8, 4, 2]))
# [2, 3, 4, 5, 8]

```

### 4. 归并排序（Merge Sort）

```python
def merge_sort(arr):
    if len(arr) <= 1:
        return arr
    mid = len(arr) // 2
    left = merge_sort(arr[:mid])
    right = merge_sort(arr[mid:])
    return merge(left, right)

def merge(left, right):
    result = []
    i = j = 0
    while i < len(left) and j < len(right):
        if left[i] <= right[j]:
            result.append(left[i])
            i += 1
        else:
            result.append(right[j])
            j += 1
    result.extend(left[i:])
    result.extend(right[j:])
    return result

print(merge_sort([5, 3, 8, 4, 2]))
# [2, 3, 4, 5, 8]
```

### 5. 堆排序

```python
def heapify(arr, n, i):
    """维护最大堆"""
    largest = i
    left = 2 * i + 1
    right = 2 * i + 2

    # 如果左子节点比当前节点大
    if left < n and arr[left] > arr[largest]:
        largest = left

    # 如果右子节点比当前节点大
    if right < n and arr[right] > arr[largest]:
        largest = right

    # 如果最大值不是当前节点，交换并继续下沉
    if largest != i:
        arr[i], arr[largest] = arr[largest], arr[i]
        heapify(arr, n, largest)


def heap_sort(arr):
    n = len(arr)

    # 构建最大堆
    for i in range(n // 2 - 1, -1, -1):
        heapify(arr, n, i)

    # 逐个取出堆顶元素，放到数组末尾
    for i in range(n - 1, 0, -1):
        arr[0], arr[i] = arr[i], arr[0]  # 交换堆顶和最后一个元素
        heapify(arr, i, 0)  # 调整剩余的堆

    return arr


# 示例
nums = [5, 3, 8, 4, 2, 7, 1]
print(heap_sort(nums))  # [1, 2, 3, 4, 5, 7, 8]

```

### 6. 二分查找

1. 迭代版

```python
def binary_search_iter(arr, target):
    left, right = 0, len(arr) - 1
    while left <= right:
        mid = (left + right) // 2
        if arr[mid] == target:
            return mid   # 找到返回索引
        elif arr[mid] < target:
            left = mid + 1
        else:
            right = mid - 1
    return -1   # 没找到返回 -1

nums = [2, 3, 4, 5, 8]
print(binary_search_iter(nums, 4))  # 2
print(binary_search_iter(nums, 7))  # -1
```

2. 递归版

```python
def binary_search_rec(arr, target, left, right):
    if left > right:
        return -1
    mid = (left + right) // 2
    if arr[mid] == target:
        return mid
    elif arr[mid] < target:
        return binary_search_rec(arr, target, mid + 1, right)
    else:
        return binary_search_rec(arr, target, left, mid - 1)

nums = [2, 3, 4, 5, 8]
print(binary_search_rec(nums, 4, 0, len(nums) - 1))  # 2
print(binary_search_rec(nums, 7, 0, len(nums) - 1))  # -1

```

