# 架构模式对比(MVC/MVVM/VIPER/TCA)

> 一句话总结：**架构模式决定状态与依赖的边界；从 MVC 到 TCA，复杂度与可测试性通常同步上升。**

---

## 1. 架构模式演进

### 1.1 MVC（Model-View-Controller）

**Apple 经典架构**：

```
┌─────────────────────────────────┐
│  View (UIView/UIViewController)   │
├─────────────────────────────────┤
│  Controller (UIViewController)   │
├─────────────────────────────────┤
│  Model (Data Model)             │
└─────────────────────────────────┘
```

**职责划分**：

| 组件 | 职责 | 示例 |
|------|------|------|
| **Model** | 数据和业务逻辑 | User, NetworkManager, DatabaseManager |
| **View** | UI 展示 | UIView, UIViewController, SwiftUI View |
| **Controller** | 协调 Model 和 View | UIViewController, Presenter |

**数据流**：

```
User Action → Controller → Model (Update)
              ↓
         View (Update)
```

**优点**：
- Apple 官方推荐，文档齐全
- 职责清晰，简单易懂
- 适合小型项目

**缺点**：
- Controller 容易臃肿（Massive View Controller）
- Model 和 View 耦合度高
- 难以测试

### 1.2 MVP（Model-View-Presenter）

**改进**：

```
┌─────────────────────────────────┐
│  View (Passive View)              │
├─────────────────────────────────┤
│  Presenter (Logic)                │
├─────────────────────────────────┤
│  Model (Data)                    │
└─────────────────────────────────┘
```

**与 MVC 的区别**：

```
MVC：Controller 同时持有 View 和 Model
MVP：Presenter 只持有 Model，View 通过协议与 Presenter 通信（被动视图）
```

**优点**：
- View 和 Model 完全解耦
- Presenter 纯逻辑，易于测试
- View 可以是 Mock 对象（单元测试）

**缺点**：
- Presenter 容易臃肿（God Object）
- 协议定义繁琐
- 代码量增加

### 1.3 MVVM（Model-View-ViewModel）

**现代架构**：

```
┌─────────────────────────────────┐
│  View (UIView/UIViewController)   │
├─────────────────────────────────┤
│  ViewModel (Bindable Model)      │
├─────────────────────────────────┤
│  Model (Data)                    │
└─────────────────────────────────┘
```

**数据绑定**：

```swift
// ViewModel
class UserViewModel: ObservableObject {
    @Published var userName: String
    @Published var userEmail: String

    private let model: UserModel

    init(model: UserModel) {
        self.model = model
        self.userName = model.name
        self.userEmail = model.email
    }

    func updateEmail(_ email: String) {
        model.email = email
        userEmail = email
    }
}

// View
struct ContentView: View {
    @StateObject private var viewModel = UserViewModel(model: UserModel())

    var body: some View {
        VStack {
            TextField("Email", text: $viewModel.userEmail)
            Text(viewModel.userName)
        }
    }
}
```

**优点**：
- ViewModel 不持有 View，解耦更好
- 支持数据绑定（Combine/RxSwift）
- ViewModel 可测试
- SwiftUI 原生支持

**缺点**：
- 学习曲线陡峭
- 数据绑定调试困难
- 过度设计风险

### 1.4 VIPER（View-Interactor-Presenter-Entity-Router）

**重型架构**：

```
┌─────────────┐  ┌──────────┐  ┌─────────┐  ┌──────┐  ┌────────┐
│  View      │←→│ Presenter│←→│Interactor│←→│Entity│  │ Router │
└─────────────┘  └──────────┘  └─────────┘  └──────┘  └────────┘
```

**职责划分**：

| 组件 | 职责 |
|------|------|
| **View** | UI 展示，用户交互 |
| **Presenter** | 协调 View 和 Interactor，处理 UI 逻辑 |
| **Interactor** | 业务逻辑，数据获取 |
| **Entity** | 数据模型（纯数据） |
| **Router** | 页面路由，导航 |

**数据流**：

```
User Action → View → Presenter → Interactor → Entity
    ↑                                      ↓
    └────────── Update ←──────────────────┘
```

**代码示例**：

```swift
// Protocol
protocol HomePresentable: class {
    var interactor: HomeInteractable? { get set }
    var router: HomeRoutable? { get set }
}

protocol HomeInteractable: class {
    var presenter: HomePresentable? { get set }
    func fetchUsers()
}

protocol HomeRoutable: class {
    func navigateToDetail(for user: User)
}

// Implementation
class HomeInteractor: HomeInteractable {
    weak var presenter: HomePresentable?
    var users = [User]()

    func fetchUsers() {
        // 获取数据
        users = [User(id: 1, name: "John")]
        presenter?.didFetchUsers(users)
    }
}

class HomePresenter: HomePresentable {
    var interactor: HomeInteractable?
    var router: HomeRoutable?

    func didFetchUsers(_ users: [User]) {
        // 更新 View
        view?.updateUsers(users)
    }
}
```

**优点**：
- 职责划分最细
- 高内聚、低耦合
- 高度可测试
- 模块化好

**缺点**：
- 代码量大
- 学习曲线陡峭
- 过度设计风险
- 小项目不适用

### 1.5 MVI（Model-View-Intent）

**单向数据流架构**：

```
┌─────────────────────────────────┐
│         User Action              │
└──────────────┬──────────────────┘
               │
      ┌────────▼────────┐
      │     Intent      │  (用户意图)
      └────────┬────────┘
               │
      ┌────────▼────────┐
      │    Reducer      │  (状态处理)
      └────────┬────────┘
               │
      ┌────────▼────────┐
      │     Model       │  (状态)
      └────────┬────────┘
               │
      ┌────────▼────────┐
      │     View        │  (UI渲染)
      └─────────────────┘
```

**核心概念**：

| 概念 | 说明 |
|------|------|
| **Intent** | 用户意图，描述用户操作 |
| **Model** | 应用状态，不可变 |
| **Reducer** | 纯函数，处理Intent返回新Model |
| **View** | 根据Model渲染UI |

**代码示例（UIKit）**：

```swift
// 1. 定义Intent（用户意图）
enum CounterIntent {
    case increment
    case decrement
}

// 2. 定义Model（状态）
struct CounterModel {
    let count: Int
    let message: String
}

// 3. 定义Reducer（状态处理）
func counterReducer(state: CounterModel, intent: CounterIntent) -> CounterModel {
    switch intent {
    case .increment:
        return CounterModel(count: state.count + 1, message: "加1")
    case .decrement:
        return CounterModel(count: state.count - 1, message: "减1")
    }
}

// 4. ViewModel
class CounterViewModel {
    @Published private(set) var model = CounterModel(count: 0, message: "")

    func handle(_ intent: CounterIntent) {
        model = counterReducer(state: model, intent: intent)
    }
}

// 5. View
class CounterViewController: UIViewController {
    private let viewModel = CounterViewModel()
    private let countLabel = UILabel()
    private let incrementButton = UIButton()
    private let decrementButton = UIButton()

    override func viewDidLoad() {
        super.viewDidLoad()
        setupUI()
        bindViewModel()
    }

    private func setupUI() {
        view.backgroundColor = .white

        countLabel.font = .systemFont(ofSize: 24)
        view.addSubview(countLabel)

        incrementButton.setTitle("+", for: .normal)
        incrementButton.addTarget(self, action: #selector(increment), for: .touchUpInside)
        view.addSubview(incrementButton)

        decrementButton.setTitle("-", for: .normal)
        decrementButton.addTarget(self, action: #selector(decrement), for: .touchUpInside)
        view.addSubview(decrementButton)

        layoutUI()
    }

    private func bindViewModel() {
        viewModel.$model.sink { [weak self] model in
            self?.countLabel.text = "\(model.count) - \(model.message)"
        }.store(in: &cancellables)
    }

    @objc private func increment() {
        viewModel.handle(.increment)
    }

    @objc private func decrement() {
        viewModel.handle(.decrement)
    }
}
```

**代码示例（SwiftUI）**：

```swift
// 1. Intent
enum CounterIntent {
    case increment
    case decrement
}

// 2. Model
struct CounterModel {
    var count: Int = 0
}

// 3. Reducer
func counterReducer(state: CounterModel, intent: CounterIntent) -> CounterModel {
    var newState = state
    switch intent {
    case .increment:
        newState.count += 1
    case .decrement:
        newState.count -= 1
    }
    return newState
}

// 4. ViewModel
class CounterViewModel: ObservableObject {
    @Published var model = CounterModel()

    func handle(_ intent: CounterIntent) {
        model = counterReducer(state: model, intent: intent)
    }
}

// 5. View
struct CounterView: View {
    @StateObject private var viewModel = CounterViewModel()

    var body: some View {
        VStack(spacing: 20) {
            Text("\(viewModel.model.count)")
                .font(.largeTitle)

            HStack(spacing: 20) {
                Button("-") { viewModel.handle(.decrement) }
                Button("+") { viewModel.handle(.increment) }
            }
        }
    }
}
```

**优点**：
- 单向数据流，状态可预测
- Intent驱动，用户操作清晰
- Reducer是纯函数，易于测试
- 适合SwiftUI
- 学习曲线比TCA平缓

**缺点**：
- 需要理解响应式编程（Combine）
- Intent定义可能过多
- Model是结构体，每次都创建新对象

**适用场景**：
- 中大型项目
- 需要清晰的数据流
- SwiftUI项目
- 团队协作

---

### 1.6 TCA (The Composable Architecture)

**SwiftUI 时代的可组合架构**：

```
┌─────────────────────────────────┐
│            State                 │  (唯一真理来源)
└──────────────┬──────────────────┘
               │
      ┌────────▼────────┐
      │  Reducer        │  (纯函数处理)
      └────────┬────────┘
               │
      ┌────────▼────────┐
      │   Store         │  (状态管理)
      └────────┬────────┘
               │
      ┌────────▼────────┐
      │    View         │  (SwiftUI View)
      └─────────────────┘
```

**核心概念**：

| 概念 | 说明 |
|------|------|
| **State** | 应用状态，唯一真理来源 |
| **Action** | 描述状态变化的事件 |
| **Reducer** | 纯函数，接收State和Action，返回新State |
| **Store** | 管理State和Reducer |
| **Effect** | 副作用，处理异步逻辑 |
| **Environment** | 外部依赖（API服务、主队列等） |

**完整代码示例**：

```swift
import ComposableArchitecture
import Combine

// 1. 定义State
struct AppState {
    var count: Int = 0
    var users: [User] = []
    var isLoading: Bool = false
    var errorMessage: String?
}

// 2. 定义Action
enum AppAction {
    case increment
    case decrement
    case fetchUsers
    case usersResponse(Result<[User], Error>)
}

// 3. 定义Environment（外部依赖）
struct AppEnvironment {
    var apiService: APIService
    var mainQueue: AnySchedulerOf<DispatchQueue>
}

// 4. 定义Reducer
let appReducer = Reducer<AppState, AppAction, AppEnvironment> {
    state, action, environment in

    switch action {
    case .increment:
        state.count += 1
        return .none

    case .decrement:
        state.count -= 1
        return .none

    case .fetchUsers:
        state.isLoading = true
        state.errorMessage = nil

        return environment.apiService.fetchUsers()
            .receive(on: environment.mainQueue)
            .catchToEffect(AppAction.usersResponse)

    case .usersResponse(.success(let users)):
        state.users = users
        state.isLoading = false
        return .none

    case .usersResponse(.failure(let error)):
        state.isLoading = false
        state.errorMessage = error.localizedDescription
        return .none
    }
}

// 5. 创建API Service
struct APIService {
    func fetchUsers() -> Effect<[User], Error> {
        return Future { promise in
            // 模拟网络请求
            DispatchQueue.main.asyncAfter(deadline: .now() + 1) {
                let users = [
                    User(id: 1, name: "张三"),
                    User(id: 2, name: "李四")
                ]
                promise(.success(users))
            }
        }
        .eraseToEffect()
    }
}

// 6. 创建Store
let store = Store(
    initialState: AppState(),
    reducer: appReducer,
    environment: AppEnvironment(
        apiService: APIService(),
        mainQueue: .main.eraseToAnyScheduler()
    )
)

// 7. SwiftUI View
struct ContentView: View {
    let store: Store<AppState, AppAction>

    var body: some View {
        WithViewStore(store) { viewStore in
            List {
                Section(header: Text("计数器")) {
                    HStack {
                        Text("Count: \(viewStore.count)")
                        Spacer()
                        Button("-") { viewStore.send(.decrement) }
                        Button("+") { viewStore.send(.increment) }
                    }
                }

                Section(header: Text("用户列表")) {
                    if viewStore.isLoading {
                        Text("加载中...")
                    } else if let errorMessage = viewStore.errorMessage {
                        Text("错误: \(errorMessage)")
                            .foregroundColor(.red)
                    } else {
                        ForEach(viewStore.users) { user in
                            Text(user.name)
                        }
                    }
                }
            }
            .navigationTitle("TCA Demo")
            .onAppear {
                viewStore.send(.fetchUsers)
            }
        }
    }
}

// 8. Preview
struct ContentView_Previews: PreviewProvider {
    static var previews: some View {
        ContentView(
            store: Store(
                initialState: AppState(),
                reducer: appReducer,
                environment: AppEnvironment(
                    apiService: APIService(),
                    mainQueue: .main.eraseToAnyScheduler()
                )
            )
        )
    }
}
```

**TCA实战案例：用户登录功能**

```swift
// 1. 登录State
struct LoginState {
    var email: String = ""
    var password: String = ""
    var isLoading: Bool = false
    var loginResponse: LoginResponse?
    var errorMessage: String?
}

// 2. 登录Action
enum LoginAction {
    case emailChanged(String)
    case passwordChanged(String)
    case loginButtonTapped
    case loginResponse(Result<LoginResponse, Error>)
}

// 3. 登录Environment
struct LoginEnvironment {
    var apiService: APIService
    var mainQueue: AnySchedulerOf<DispatchQueue>
}

// 4. 登录Reducer
let loginReducer = Reducer<LoginState, LoginAction, LoginEnvironment> {
    state, action, environment in

    switch action {
    case .emailChanged(let email):
        state.email = email
        return .none

    case .passwordChanged(let password):
        state.password = password
        return .none

    case .loginButtonTapped:
        state.isLoading = true
        state.errorMessage = nil

        return environment.apiService.login(email: state.email, password: state.password)
            .receive(on: environment.mainQueue)
            .catchToEffect(LoginAction.loginResponse)

    case .loginResponse(.success(let response)):
        state.isLoading = false
        state.loginResponse = response
        return .none

    case .loginResponse(.failure(let error)):
        state.isLoading = false
        state.errorMessage = error.localizedDescription
        return .none
    }
}

// 5. 登录View
struct LoginView: View {
    let store: Store<LoginState, LoginAction>

    var body: some View {
        WithViewStore(store) { viewStore in
            Form {
                TextField("邮箱", text: viewStore.binding(get: \.email, send: LoginAction.emailChanged))
                    .textInputAutocapitalization(.never)
                    .keyboardType(.emailAddress)

                SecureField("密码", text: viewStore.binding(get: \.password, send: LoginAction.passwordChanged))

                if viewStore.isLoading {
                    ProgressView()
                } else {
                    Button("登录") {
                        viewStore.send(.loginButtonTapped)
                    }
                    .disabled(!viewStore.isValidForm)
                }

                if let errorMessage = viewStore.errorMessage {
                    Text(errorMessage)
                        .foregroundColor(.red)
                }
            }
            .navigationTitle("登录")
        }
    }
}

// ViewStore扩展
extension LoginState {
    var isValidForm: Bool {
        return !email.isEmpty && !password.isEmpty && password.count >= 6
    }
}
```

**优点**：
- 单向数据流，状态可预测
- 纯函数Reducer，易于测试
- 可组合性强，Reducer可以组合
- 适合SwiftUI
- 支持时间旅行调试
- 状态管理统一

**缺点**：
- 学习曲线陡峭
- 样板代码多
- 过度设计风险
- 调试相对困难

**适用场景**：
- 大型SwiftUI项目
- 需要严格状态管理
- 团队协作（多人开发）
- 需要时间旅行调试

---

### 1.7 Clean Architecture（整洁架构）

**依赖倒置的分层架构**：

```
┌─────────────────────────────────────────┐
│       Frameworks & Drivers              │  ← 外层（UIKit、Foundation）
│  (Interface Adapters, UI, Devices)       │
├─────────────────────────────────────────┤
│           Use Cases                     │  ← 中层（业务逻辑）
│   (Application Business Rules)           │
├─────────────────────────────────────────┤
│         Entities                        │  ← 内层（核心业务规则）
│   (Enterprise Business Rules)            │
└─────────────────────────────────────────┘
```

**核心原则**：
- **依赖倒置**：外层依赖内层，内层不知道外层
- **独立于框架**：业务逻辑不依赖UIKit、Foundation
- **独立于UI**：业务逻辑不依赖UI框架
- **独立于数据库**：业务逻辑不依赖数据库
- **可测试**：所有层都可以独立测试

**iOS中的Clean Architecture**：

```
┌─────────────────────────────────────────┐
│          Presentation Layer             │  (UI、ViewModel)
├─────────────────────────────────────────┤
│            Domain Layer                  │  (Entity、Use Case)
├─────────────────────────────────────────┤
│            Data Layer                    │  (Repository、Data Source)
└─────────────────────────────────────────┘
```

**代码示例**：

```swift
// 1. Domain Layer（业务层，最核心）

// Entity（实体）
struct User {
    let id: String
    let name: String
    let email: String
}

// Use Case（用例协议）
protocol FetchUsersUseCase {
    func execute() -> AnyPublisher<[User], Error>
}

// Use Case实现
class FetchUsersUseCaseImpl: FetchUsersUseCase {
    private let userRepository: UserRepository

    init(userRepository: UserRepository) {
        self.userRepository = userRepository
    }

    func execute() -> AnyPublisher<[User], Error> {
        return userRepository.fetchUsers()
    }
}

// Repository协议（仓库）
protocol UserRepository {
    func fetchUsers() -> AnyPublisher<[User], Error>
    func fetchUser(id: String) -> AnyPublisher<User, Error>
}

// 2. Data Layer（数据层）

// Repository实现
class UserRepositoryImpl: UserRepository {
    private let apiService: APIService
    private let cacheService: CacheService

    init(apiService: APIService, cacheService: CacheService) {
        self.apiService = apiService
        self.cacheService = cacheService
    }

    func fetchUsers() -> AnyPublisher<[User], Error> {
        // 先从缓存读取
        if let cachedUsers = cacheService.loadUsers() {
            return Just(cachedUsers)
                .setFailureType(to: Error.self)
                .eraseToAnyPublisher()
        }

        // 缓存没有，从网络获取
        return apiService.fetchUsers()
            .handleEvents(receiveOutput: { users in
                self.cacheService.saveUsers(users)
            })
            .eraseToAnyPublisher()
    }

    func fetchUser(id: String) -> AnyPublisher<User, Error> {
        return apiService.fetchUser(id: id)
    }
}

// 3. Presentation Layer（展示层）

// ViewModel
class UserListViewModel: ObservableObject {
    @Published var users: [User] = []
    @Published var isLoading: Bool = false
    @Published var errorMessage: String?

    private let fetchUsersUseCase: FetchUsersUseCase
    private var cancellables = Set<AnyCancellable>()

    init(fetchUsersUseCase: FetchUsersUseCase) {
        self.fetchUsersUseCase = fetchUsersUseCase
    }

    func fetchUsers() {
        isLoading = true
        errorMessage = nil

        fetchUsersUseCase.execute()
            .receive(on: DispatchQueue.main)
            .sink { completion in
                self.isLoading = false
            } receiveValue: { users in
                self.users = users
            }
            .store(in: &cancellables)
    }
}

// SwiftUI View
struct UserListView: View {
    @StateObject private var viewModel: UserListViewModel

    init(viewModel: UserListViewModel) {
        self._viewModel = StateObject(wrappedValue: viewModel)
    }

    var body: some View {
        List {
            if viewModel.isLoading {
                ProgressView()
            } else if let errorMessage = viewModel.errorMessage {
                Text("错误: \(errorMessage)")
                    .foregroundColor(.red)
            } else {
                ForEach(viewModel.users) { user in
                    Text(user.name)
                }
            }
        }
        .navigationTitle("用户列表")
        .onAppear {
            viewModel.fetchUsers()
        }
    }
}

// 4. Dependency Injection（依赖注入）

// DI Container
class DIContainer {
    static let shared = DIContainer()

    private init() {}

    lazy var apiService: APIService = {
        return APServiceImpl()
    }()

    lazy var cacheService: CacheService = {
        return CacheServiceImpl()
    }()

    lazy var userRepository: UserRepository = {
        return UserRepositoryImpl(
            apiService: apiService,
            cacheService: cacheService
        )
    }()

    lazy var fetchUsersUseCase: FetchUsersUseCase = {
        return FetchUsersUseCaseImpl(
            userRepository: userRepository
        )
    }()

    func makeUserListViewModel() -> UserListViewModel {
        return UserListViewModel(
            fetchUsersUseCase: fetchUsersUseCase
        )
    }
}

// 5. 使用
struct ContentView: View {
    var body: some View {
        UserListView(
            viewModel: DIContainer.shared.makeUserListViewModel()
        )
    }
}
```

**Clean Architecture实战：登录功能**

```swift
// 1. Domain Layer

// Entity
struct LoginCredentials {
    let email: String
    let password: String
}

struct AuthToken {
    let token: String
    let expiresAt: Date
}

// Use Case
protocol LoginUseCase {
    func execute(credentials: LoginCredentials) -> AnyPublisher<AuthToken, Error>
}

class LoginUseCaseImpl: LoginUseCase {
    private let authRepository: AuthRepository

    init(authRepository: AuthRepository) {
        self.authRepository = authRepository
    }

    func execute(credentials: LoginCredentials) -> AnyPublisher<AuthToken, Error> {
        // 验证邮箱格式
        guard credentials.email.contains("@") else {
            return Fail(error: AuthError.invalidEmail)
                .eraseToAnyPublisher()
        }

        // 验证密码长度
        guard credentials.password.count >= 6 else {
            return Fail(error: AuthError.passwordTooShort)
                .eraseToAnyPublisher()
        }

        // 调用Repository
        return authRepository.login(credentials)
    }
}

// Repository协议
protocol AuthRepository {
    func login(_ credentials: LoginCredentials) -> AnyPublisher<AuthToken, Error>
    func logout() -> AnyPublisher<Void, Error>
}

// 2. Data Layer

// Repository实现
class AuthRepositoryImpl: AuthRepository {
    private let apiService: APIService
    private let tokenStorage: TokenStorage

    init(apiService: APIService, tokenStorage: TokenStorage) {
        self.apiService = apiService
        self.tokenStorage = tokenStorage
    }

    func login(_ credentials: LoginCredentials) -> AnyPublisher<AuthToken, Error> {
        return apiService.login(email: credentials.email, password: credentials.password)
            .handleEvents(receiveOutput: { token in
                self.tokenStorage.save(token)
            })
            .eraseToAnyPublisher()
    }

    func logout() -> AnyPublisher<Void, Error> {
        return apiService.logout()
            .handleEvents(receiveOutput: {
                self.tokenStorage.clear()
            })
            .eraseToAnyPublisher()
    }
}

// 3. Presentation Layer

// ViewModel
class LoginViewModel: ObservableObject {
    @Published var email: String = ""
    @Published var password: String = ""
    @Published var isLoading: Bool = false
    @Published var errorMessage: String?
    @Published var isLoggedIn: Bool = false

    private let loginUseCase: LoginUseCase

    init(loginUseCase: LoginUseCase) {
        self.loginUseCase = loginUseCase
    }

    func login() {
        let credentials = LoginCredentials(email: email, password: password)
        isLoading = true
        errorMessage = nil

        loginUseCase.execute(credentials: credentials)
            .receive(on: DispatchQueue.main)
            .sink { completion in
                self.isLoading = false
            } receiveValue: { [weak self] _ in
                self?.isLoggedIn = true
            } failure: { [weak self] error in
                self?.errorMessage = error.localizedDescription
            }
            .store(in: &cancellables)
    }
}

// View
struct LoginView: View {
    @StateObject private var viewModel: LoginViewModel

    init(viewModel: LoginViewModel) {
        self._viewModel = StateObject(wrappedValue: viewModel)
    }

    var body: some View {
        Form {
            TextField("邮箱", text: $viewModel.email)
            SecureField("密码", text: $viewModel.password)

            if viewModel.isLoading {
                ProgressView()
            } else {
                Button("登录") {
                    viewModel.login()
                }
                .disabled(!viewModel.isValidForm)
            }

            if let errorMessage = viewModel.errorMessage {
                Text(errorMessage)
                    .foregroundColor(.red)
            }
        }
        .navigationTitle("登录")
    }
}
```

**优点**：
- 业务逻辑独立，易于测试
- 依赖倒置，高层不依赖低层
- 各层职责清晰
- 易于维护和扩展
- 适合大型项目

**缺点**：
- 代码量多
- 学习曲线陡
- 过度设计风险
- 小项目不适用

**适用场景**：
- 大型项目
- 需要长期维护
- 团队协作
- 业务逻辑复杂

---

## 2. 架构模式对比

### 2.1 横向对比

| 特性 | MVC | MVP | MVVM | MVI | VIPER | TCA | Clean Architecture |
|------|-----|-----|------|-----|-------|-----|-------------------|
| **复杂度** | ⭐ | ⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **可测试性** | ⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **可维护性** | ⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **学习曲线** | ⭐ | ⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **代码量** | ⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **适用项目** | 小型 | 中型 | 中大型 | 中大型 | 大型 | 大型 | 大型 |
| **SwiftUI 支持** | ⭐⭐⭐ | ⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| **数据流** | 双向 | 双向 | 双向 | 单向 | 单向 | 单向 | 双向 |
| **状态管理** | 分散 | 分散 | ViewModel | Model | Store | Store | UseCase |

### 2.2 如何选择？

**项目规模**：

```
小型项目（< 5 个页面）：
→ MVC

中型项目（5-20 个页面）：
→ MVVM + Combine
→ 或 MVI（如果需要单向数据流）

大型项目（> 20 个页面）：
→ UIKit：VIPER 或 MVVM + Module
→ SwiftUI：TCA 或 Clean Architecture
→ 团队协作：VIPER / Clean Architecture
→ 个人开发：MVVM / MVI
```

**团队规模**：

```
1-2 人：MVC 或 MVVM
3-5 人：MVVM + Module 或 MVI
5+ 人：VIPER / Clean Architecture / TCA
```

**技术栈**：

```
UIKit：
→ MVC（传统）
→ MVVM（推荐，结合Combine）
→ MVI（单向数据流）
→ VIPER（大型项目）
→ Clean Architecture（业务复杂）

SwiftUI：
→ MVVM（原生支持）
→ MVI（推荐，学习曲线平缓）
→ TCA（严格状态管理）
→ Clean Architecture（业务复杂）
```

**业务复杂度**：

```
业务简单（CRUD）：
→ MVC / MVVM

业务中等（有状态管理）：
→ MVVM / MVI

业务复杂（多状态、异步操作）：
→ TCA / Clean Architecture

业务非常复杂（多模块、多交互）：
→ Clean Architecture + VIPER
```

**何时选择MVI？**
✅ 需要单向数据流，但TCA太复杂
✅ SwiftUI项目，需要清晰的状态管理
✅ 中型项目，团队能力中等
❌ 超大型项目（考虑TCA或Clean Architecture）

**何时选择TCA？**
✅ 大型SwiftUI项目
✅ 需要严格的状态管理
✅ 需要时间旅行调试
✅ 团队有函数式编程经验
❌ 小项目（过度设计）
❌ 团队Swift经验不足

**何时选择Clean Architecture？**
✅ 业务逻辑复杂
✅ 需要长期维护
✅ 多人协作
✅ 需要高可测试性
❌ 小项目（过度设计）
❌ 快速原型（开发慢）

---

### 6.1 架构模式

| 问题 | 答案要点 | 难度 |
|------|----------|------|
| **MVC 的缺点？** | Massive View Controller，难以测试 | ⭐⭐⭐ |
| **MVVM 的优势？** | ViewModel 可测试，支持数据绑定 | ⭐⭐⭐⭐ |
| **VIPER 的缺点？** | 代码量大，学习曲线陡 | ⭐⭐⭐ |
| **MVC vs MVVM vs VIPER？** | MVC：简单；MVVM：数据绑定；VIPER：高内聚 | ⭐⭐⭐⭐ |
| **什么是MVI？** | 单向数据流架构，Intent驱动状态变化 | ⭐⭐⭐⭐ |
| **TCA的核心概念？** | State、Action、Reducer、Store、Effect | ⭐⭐⭐⭐⭐ |
| **Clean Architecture的原则？** | 依赖倒置，外层依赖内层，业务逻辑独立 | ⭐⭐⭐⭐⭐ |
| **何时选择TCA vs MVVM？** | TCA：大型SwiftUI项目；MVVM：中型项目 | ⭐⭐⭐⭐ |

### 6.2 设计模式

| 问题 | 答案要点 | 难度 |
|------|----------|------|
| **Delegate 的作用？** | 事件回调，解耦 | ⭐⭐⭐ |
| **Singleton 的使用场景？** | 全局唯一，网络管理、数据库管理 | ⭐⭐⭐ |
| **Observer 的实现方式？** | 通知中心、KVO、Combine | ⭐⭐⭐⭐ |
| **Adapter 的应用场景？** | 接口不兼容，数据转换 | ⭐⭐⭐ |

### 6.3 SOLID 原则

| 问题 | 答案要点 | 难度 |
|------|----------|------|
| **单一职责原则？** | 一个类只负责一件事 | ⭐⭐⭐ |
| **开闭原则？** | 对扩展开放，对修改关闭 | ⭐⭐⭐⭐ |
| **里氏替换原则？** | 子类可替换父类 | ⭐⭐⭐⭐ |
| **依赖倒置原则？** | 依赖抽象而非具体 | ⭐⭐⭐⭐ |

---


---

## 7. 参考资料

### 优质文章
- [iOS Architecture Patterns. Demystifying MVC, MVP, MVVM and VIPER](https://medium.com/ios-os-x-development/ios-architecture-patterns-ecba4c38de52)
- [iOS Architecture Patterns: MVC, MVP, MVVM, VIPER, and VIP in Swift (2026)](https://www.oreilly.com/library/view/ios-architecture-patterns/9781484290682/)
- [The perfect iOS app architecture](https://betterprogramming.com/the-perfect-ios-app-architecture-mvvm-viper-clean-swift/)
- [Why VIPER and MVVM in SwiftUI are actually the same pattern](https://matteomanferdini.com/2021/09/26/why-viper-and-mvvm-in-swiftui-are-actually-the-same-pattern/)

### 开源项目
- [Hero (iOS Architecture Pattern)](https://github.com/netguru/Hero) - MVVM+Coordinator
- [RxFlow](https://github.com/RxSwiftCommunity/RxFlow) - Coordinated navigation
- [TCA (The Composable Architecture)](https://github.com/pointfreeco/The-Composable-Architecture)

---

**最后更新**：2026-05-23
**状态**：✅ 已完善（补充MVI、TCA、Clean Architecture）

**Sources:**
- [iOS Architecture Patterns. Demystifying MVC, MVP, MVVM and VIPER](https://medium.com/ios-os-x-development/ios-architecture-patterns-ecba4c38de52)
- [iOS Architecture Patterns (2026)](https://www.oreilly.com/library/view/ios-architecture-patterns/9781484290682/)
- [The perfect iOS app architecture](https://betterprogramming.com/the-perfect-ios-app-architecture-mvvm-viper-clean-swift/)
- [Point-Free: TCA Documentation](https://www.pointfree.co/blog/posts/the-composable-architecture/)
- [Clean Architecture by Robert Martin](https://blog.cleancoder.com/uncle-bob/2012/08/13/the-clean-architecture.html)
