以下基于你的最终简历，按**面试命中率 × 简历强相关度 × 准备投入产出比**三个维度，从高到低列出准备优先级。

---

## 🔴 第一优先级（必考，必须深挖）

### 1. Runtime 底层原理与实战

**为什么必考**：Runtime 是 iOS 高级面试的“定海神针”，几乎所有大厂技术面第一轮必问。高级岗不会只问“什么是Runtime”，而是会交叉追问，考察你对对象模型、消息转发、内存管理的系统性理解。

**简历中的对应证据**：
- “利用RunTime特性对线上崩溃进行防护”
- “封装插件化框架TPPageLife”、“封装依赖注入框架TPSwinject”
- “熟悉iOS开发的知识体系和底层原理”

**必须准备的问题**：
- `objc_msgSend` 的完整执行流程是什么？为什么用汇编实现？
- 对象、类对象、元类对象的关系是什么？
- 消息转发（Message Forwarding）的完整流程：`resolveInstanceMethod` → `forwardingTargetForSelector` → `methodSignatureForSelector` + `forwardInvocation`
- Method Swizzling 的实现原理、注意事项（线程安全、影响范围、+load vs +initialize）
- 关联对象（Associated Object）的实现原理，什么时候需要手动释放？
- 分类（Category）的本质：编译后生成的结构体、加载时机、与类扩展的区别
- 能否向编译后的类中添加实例变量？能否向运行时创建的类中添加？为什么？

**深度追问准备**（高级岗拉分项）：
- Runtime 如何影响 KVO、Category、Swizzling 的底层实现？
- 方法缓存（Method Cache）的结构和查找流程
- 如果你来实现一个 `weak` 变量，你会怎么设计？


### 2. RunLoop 原理与实战应用

**为什么必考**：RunLoop 是 iOS 事件循环的核心，面试官常用来考察你对主线程机制、卡顿检测、线程保活的理解深度。你的简历里直接写了“利用Runloop原理开发卡顿检测组件”，这个点一定会被追问。

**简历中的对应证据**：
- “利用Runloop原理开发卡顿检测组件，对项目中的列表滑动场景进行检测”
- “封装FOOM检测组件”

**必须准备的问题**：
- RunLoop 的本质是什么？与线程的关系是什么？
- RunLoop 的 Mode 有哪些？`NSDefaultRunLoopMode`、`UITrackingRunLoopMode`、`NSRunLoopCommonModes` 的区别和使用场景
- Source0 和 Source1 的区别是什么？
- Timer 在 UIScrollView 滑动时失效的原因和解决方案
- RunLoop 与 AutoreleasePool 的关系是什么？
- 如何利用 RunLoop 实现线程保活（常驻线程）？
- **重点**：卡顿检测组件的实现原理——怎么监测主线程 RunLoop 的耗时？Observer 监听哪些状态？阈值怎么设？误报怎么处理？

**深度追问准备**：
- GCD 的 dispatch_async 到主队列时，底层是如何唤醒 RunLoop 的？
- 为什么在 `viewDidLoad` 里做大循环要手动加 `@autoreleasepool`？


### 3. 内存管理（ARC/MRC、循环引用、FOOM）

**为什么必考**：内存管理是 iOS 开发的基石，高级岗会从“是什么”问到“为什么”，再到“怎么系统性治理”。你的简历里有“内存泄露检测与治理”、“FOOM从0.7%降至0.25%”的具体数据，这是极好的谈资。

**简历中的对应证据**：
- “封装FOOM检测组件，并对项目内存泄露问题进行长期优化和治理”
- “FOOM从最初的0.7%左右降到目前0.25%左右”

**必须准备的问题**：
- ARC 和 MRC 的本质区别是什么？ARC 在编译期做了什么？
- 循环引用的典型场景：Block 捕获 self、Delegate 强引用、NSTimer 不释放
- `weak` 的实现原理：SideTable、弱引用表、自动置 nil 的时机
- `strong`、`copy`、`assign`、`weak` 的区别和使用场景
- **重点**：FOOM（Foreground Out-Of-Memory）是什么？为什么普通崩溃监控抓不到？你是怎么监控和治理的？
- 内存泄漏的检测工具和方法：Instruments Leaks、Malloc Debug、自己实现的内存水位监控

**深度追问准备**：
- AutoreleasePool 的原理是什么？对象什么时候会被释放？
- 什么是“野指针”？如何防护？【简历中有“野指针治理”】
- 如何定位一个大对象（如图片）的内存占用来源？


## 🟡 第二优先级（高频，必须准备）

### 4. 多线程与 GCD

**为什么重要**：多线程是高级 iOS 的必考点，面试官会通过它考察你对并发模型、线程安全、死锁的理解。虽然你简历里没单独列“多线程”，但性能优化、卡顿治理都离不开这个基础。

**必须准备的问题**：
- GCD 的队列类型：串行/并发、主队列/全局队列
- `sync` vs `async` 的区别，什么场景下会死锁？
- 栅栏函数（`dispatch_barrier`）的使用场景
- 信号量（`dispatch_semaphore`）的作用和注意事项
- 多个网络请求完成后统一通知的方式有哪些？
- 线程安全的实现方式：锁（`@synchronized`、`NSLock`、`dispatch_semaphore`）、原子属性


### 5. 性能优化体系（崩溃、卡顿、启动、包体积）

**为什么重要**：你简历里最硬的成绩就是性能治理（崩溃率0.23%→0.01%，FOOM 0.7%→0.25%），面试官一定会围绕这个深挖。这是你区别于普通iOS开发的核心卖点。

**简历中的对应证据**：
- “崩溃率从最初0.23%降到到目前0.01%左右”
- “FOOM从最初的0.7%左右降到目前0.25%左右”
- “卡顿检测组件”、“性能相关数据自动告警”

**必须准备的问题**：
- **崩溃治理**：
  - iOS 常见的崩溃类型有哪些？（NSException、Signal 崩溃、野指针、OOM）
  - 野指针崩溃的根因是什么？如何防护？（你的简历写了“利用RunTime特性对线上崩溃进行防护”，具体怎么做的？）
  - Method Swizzling 做崩溃防护的优缺点是什么？有没有副作用？
- **卡顿优化**：
  - 卡顿检测的原理（基于 RunLoop Observer）
  - 列表滑动卡顿的常见原因和优化手段（Cell 复用、高度预计算、离屏渲染、图片解码）
  - 离屏渲染是什么？为什么会造成性能问题？
- **启动优化**：
  - App 启动流程（main() 之前和之后）
  - 如何监控和优化启动时间？
- **包体积优化**：
  - 资源文件优化、代码瘦身（无用类/方法检测）


### 6. 架构设计与重构（组件化、插件化、MVVM）

**为什么重要**：10年经验的定位就是“能带项目、能定架构”，架构设计是高级岗和中级岗的分水岭。你的简历里有完整的插件化重构案例，这是面试中展示架构思维的最佳素材。

**简历中的对应证据**：
- “封装插件化框架TPPageLife”、“封装依赖注入框架TPSwinject”
- “熟悉常用的设计模式、设计原则、主流架构，有不同架构的实际项目经验”
- “重构项目整体框架，支持基础框架层能够服务iPad客户端”
- “从0～1独立完成整个app的开发和上线工作，使用了MVVM的架构”

**必须准备的问题**：
- **插件化架构**：
  - 为什么选择插件化而不是组件化？解决什么痛点？
  - 插件之间的通信方式有哪些？（依赖注入 vs 通知 vs 路由 vs 协议）各自优缺点是什么？
  - 如何管理插件的生命周期？和 VC 生命周期的关系是什么？
  - 插件如何做到独立编译和调试？
- **设计模式**：
  - 你项目中用到的设计模式有哪些？（策略模式、依赖注入、外观模式等）
  - 策略模式在消息展示框架 TPMessageKit 中具体怎么用的？
- **MVVM**：
  - MVVM 和 MVC 的本质区别是什么？
  - ViewModel 和 View 的绑定如何实现？（KVO、RAC、Combine）
  - MVVM 的缺点是什么？什么场景不适合？


## 🟢 第三优先级（差异化加分项，建议准备）

### 7. AI 辅助开发与工程效能

**为什么是加分项**：你的简历中“AI辅助开发”是相比其他10年iOS工程师的差异化亮点。2026年面试中，AI工具的使用经验越来越被看重，但很多人只是“用过”，你能“沉淀规范、固化流程”就是高级感。

**简历中的对应证据**：
- “深度使用Cursor/Claude Code完成从需求到上线的全流程开发”
- “沉淀CLAUDE.md、组件规范、OpenSpec等标准化文档，使AI能按项目规范自动生成代码并执行Review”
- “将提效经验固化为团队工作流”

**建议准备的问题**：
- 你是怎么用 AI 辅助日常开发的？具体流程是什么？
- AI 生成的代码你怎么做质量把控？遇到过什么问题？
- CLAUDE.md、OpenSpec 这些规范文档具体包含什么内容？为什么能让 AI 写出更好的代码？
- 你认为 AI 会取代 iOS 开发吗？你怎么看待 AI 时代工程师的价值？


### 8. App Store 审核与合规（红人点点）

**为什么是加分项**：处理过苹果 3.2.0 下架并成功重新上线的经验，在国内开发者中非常稀缺。面试官如果对这块感兴趣，会成为你独特的谈资。

**简历中的对应证据**：
- “由于老版本违反了苹果审核的3.2.0规则...我接管项目后使用各种马甲包、代码混淆、关键字处理、敏感信息加密等方法将其成功上线”

**建议准备的问题**：
- 3.2.0 规则具体是什么？为什么会被下架？
- 马甲包、代码混淆、敏感信息加密具体是怎么做的？
- 审核被拒的常见原因和应对策略有哪些？
- 如何设计“审核环境”和“生产环境”分离？


### 9. Swift & SwiftUI（MusicFriends 项目）

**为什么需要准备**：你的最新项目用了 SwiftUI，虽然面试官不一定要求你精通，但至少会问“为什么选 SwiftUI”、“和 UIKit 的混编怎么处理”。

**简历中的对应证据**：
- “MusicFriends ... SwiftUI”
- “于斯课堂 4.0 版本整体重构，OC-Swift混编”

**建议准备的问题**：
- SwiftUI 和 UIKit 的区别是什么？什么场景适合 SwiftUI？
- SwiftUI 和 UIKit 如何混编？（UIHostingController、UIViewRepresentable）
- Swift 的值类型和引用类型区别？Copy-on-Write 是什么？


## 📋 总结：准备优先级一览表

| 优先级 | 模块 | 简历强相关度 | 面试命中率 | 建议投入时间 |
|:---:|:---|:---:|:---:|:---:|
| 🔴 P0 | Runtime 底层原理 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | 最多 |
| 🔴 P0 | RunLoop 原理与卡顿检测 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | 最多 |
| 🔴 P0 | 内存管理与 FOOM 治理 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | 最多 |
| 🟡 P1 | 多线程与 GCD | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | 中等 |
| 🟡 P1 | 性能优化体系 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | 中等 |
| 🟡 P1 | 架构设计与重构 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | 中等 |
| 🟢 P2 | AI 辅助开发 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | 少量 |
| 🟢 P2 | App Store 审核合规 | ⭐⭐⭐ | ⭐⭐ | 少量 |
| 🟢 P2 | Swift & SwiftUI | ⭐⭐⭐ | ⭐⭐⭐ | 少量 |

---

**最后的建议**：P0 的三个模块（Runtime、RunLoop、内存管理）是“防淘汰”的，不准备到位连第一轮都过不去。P1 是“拉差距”的，决定你能不能拿到后续面试。P2 是“加分项”，准备好了是惊喜，没时间可以暂时放一放。

你在实际准备某个具体问题时遇到卡点，可以随时来找我深挖。